import { DropSnakeCasePipe } from './drop-snake-case.pipe';

describe('DropSnakeCasePipe', () => {
  it('create an instance', () => {
    const pipe = new DropSnakeCasePipe();
    expect(pipe).toBeTruthy();
  });
});
